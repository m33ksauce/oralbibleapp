language: node_js
node_js:
  - "15.5.0"

branches:
  only:
  - master
  - dev

before_install:
  - npm install -g @ionic/cli @angular/cli

install:
  - cd OpenOralBibleClient
  - npm ci

jobs:
  include:
  - stage: test
    script: npm run ci-test